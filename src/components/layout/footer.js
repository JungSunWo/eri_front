'use client'

import React, { useEffect, useState} from 'react';


const FooterLayout = () => {


		useEffect(() => {
			const drawFooter = (showYn) => {

				return(
					<div></div>
				)
			}


			return (
				drawFooter(false)
			)
		});


};

export default FooterLayout;
